{% extends 'base.html.twig' %}

{% block title %}Nationalite{% endblock %}

{% block body %}
    <h1>Nationalite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nationalite.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ nationalite.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nationalite_index') }}">back to list</a>

    <a href="{{ path('app_nationalite_edit', {'id': nationalite.id}) }}">edit</a>

    {{ include('nationalite/_delete_form.html.twig') }}
{% endblock %}
