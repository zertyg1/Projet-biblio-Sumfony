<form method="post" action="{{ path('app_nationalite_delete', {'id': nationalite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nationalite.id) }}">
    <button class="btn">Delete</button>
</form>
